baseURL = 'http://example.org/'
languageCode = 'zh-cn'
title = "buvidk's Blog"
theme = 'PaperMod'

[params]
    mermaid = true
    math = true

    [params.homeInfoParams]
        Title = "buvidk's Blog"
        Content = "You don't need to be a great artist to be great at art."

    [[params.socialIcons]]
        name = 'github'
        title = 'GitHub'
        url = 'https://github.com/buvidk1234'

[outputs]
    home = ['HTML', 'RSS', 'JSON']

[menu]
    [[menu.main]]
        identifier = 'archives'
        name = 'Archive'
        url = '/archives/'
        weight = 10

    [[menu.main]]
        identifier = 'search'
        name = 'Search'
        url = '/search/'
        weight = 20

    [[menu.main]]
        identifier = 'tags'
        name = 'Tags'
        url = '/tags/'
        weight = 30

[markup]
    [markup.highlight]
        noClasses = false
        style = 'github'
    [markup.goldmark]
        [markup.goldmark.extensions]
            [markup.goldmark.extensions.passthrough]
                enable = true
                [markup.goldmark.extensions.passthrough.delimiters]
                    block = [['$$', '$$'], ['\[', '\]']]
                    inline = [['$', '$'], ['\(', '\)']]
